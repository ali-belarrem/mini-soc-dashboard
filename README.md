# mini-soc-dashboard
A web dashboard built with Flask that unifies three security tools — network discovery, system hardening audit, and log-based threat detection — into a single mini SOC interface.
